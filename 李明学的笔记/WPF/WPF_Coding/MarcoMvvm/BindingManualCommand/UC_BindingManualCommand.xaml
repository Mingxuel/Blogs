<UserControl x:Class="MarcoMVVM.UC_BindingManualCommand"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:MarcoMVVM"
             mc:Ignorable="d" 
             Width="450" Height="200" BorderThickness="1" BorderBrush="Black">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition/>
            <ColumnDefinition/>
            <ColumnDefinition/>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition/>
            <RowDefinition/>
            <RowDefinition/>
            <RowDefinition/>
        </Grid.RowDefinitions>
        <TextBox Grid.Row="1" Grid.Column="1" Grid.ColumnSpan="2" x:Name="textBox" Text="{Binding MarcoInput}" Height="40"/>
        <Button Grid.Row="2" Grid.Column="1" Content="无参函数" Command="{Binding NoParamCommand}" Height="40" Margin=" 10" VerticalAlignment="Center"/>
        <Button Grid.Row="2" Grid.Column="2" Content="有参参数" Command="{Binding ParamCommand}" CommandParameter="{Binding ElementName=textBox, Path=Text}" Height="40" Margin="10" VerticalAlignment="Center"/>
    </Grid>
</UserControl>
