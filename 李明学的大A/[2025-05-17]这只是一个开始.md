# <span style="color: #FFA000;">我的大A记录</span> 

## 代码展示示例

行内代码示例：这是一个`print("Hello World!")`命令

代码块示例：
```python
def calculate_profit(buy_price, sell_price, shares):
    profit = (sell_price - buy_price) * shares
    return profit

# 计算收益
result = calculate_profit(10.5, 11.2, 100)
print(f"收益为: {result}元")
```

也可以不指定语言：
```
这是一个普通的代码块
不会有语法高亮
可以用来展示任何格式化文本
```